# Enhanced SEO Audit Tool V3.0 - Python 3.13 Emergency Compatibility
# Last resort if we can't force Python 3.11

# Core Flask Framework
Flask==3.0.0
Flask-CORS==4.0.0
Werkzeug==3.0.1

# HTTP and Web Scraping (Python 3.13 compatible versions)
aiohttp==3.9.1
beautifulsoup4==4.12.2
lxml==5.1.0
requests==2.31.0

# URL and Data Validation
validators==0.22.0

# Data Processing and Analysis (avoid problematic pandas/numpy)
# pandas==2.1.4  # Commented out - causes build issues
# numpy==1.26.2   # Commented out - causes build issues

# Text Processing and Analysis
textstat==0.7.3

# URL Processing
urllib3==2.1.0

# Production WSGI Server
gunicorn==21.2.0

# Environment Management
python-dotenv==1.0.0

# CRITICAL: Do not include setuptools or wheel in requirements.txt
# These will be handled by the runtime environment
